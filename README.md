# homebrew-terminale

Homebrew tap for [**terminale**](https://github.com/fbrzlarosa/terminale) — a
native, cross-platform, GPU-accelerated terminal.

## Install

```sh
brew install fbrzlarosa/terminale/terminale
```

The formula in this tap is published automatically by terminale's release
pipeline on every tagged release.
